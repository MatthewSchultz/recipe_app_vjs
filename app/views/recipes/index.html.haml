%h1 Recipes

%table.table
  %thead
    %tr
      %th Title
      %th Cuisine
      %th Planning to cook on
      %th Tried before
      %th Instructions
      %th
      %th
      %th

  %tbody
    - @recipes.each do |recipe|
      %tr
        %td= recipe.title
        %td= recipe.cuisine
        %td= recipe.planning_to_cook_on
        %td= recipe.tried_before
        %td= recipe.instructions
        %td= link_to 'Show', recipe
        %td= link_to 'Edit', edit_recipe_path(recipe)
        %td= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Recipe', new_recipe_path
